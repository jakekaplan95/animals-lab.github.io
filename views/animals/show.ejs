<!DOCTYPE html>
<html lang="en">
<body>
    <main>=
        <div>
            <article>
                <h2>
                <%= animal.species %> - <%= animal.extinct ? "Extinct" :
                "Not extinct" %>
                </h2>
                <h3>
                    <%= animal.loction %>
                </h3>
                <a href="/animals/<%= animal._id %>/edit"><button>Edit</button></a>
                <form action="/animals/<%= animal._id %>?_method=DELETE "
                    method="post">
                <input type = "submit" value="Delete" />
                </form>
            </article>
        </div>
    </main>

</body>
</html>